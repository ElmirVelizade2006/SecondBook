@extends('layout.admin.master')
@section('title', 'Edit Refund')
@section('content')
<div class="dashboard-section refunds-page"><div class="dashboard-panel mb-4"><div class="panel-header"><div><span class="eyebrow">Payments recovery</span><h5>Edit refund</h5><p class="text-muted mb-0">{{ $refund->refund_number }}</p></div><a href="{{ route('admin.refunds.show', $refund) }}" class="btn btn-light border"><i class="bi bi-arrow-left me-2"></i>Back</a></div></div><div class="dashboard-panel refund-form-panel"><form method="POST" action="{{ route('admin.refunds.update', $refund) }}">@csrf @method('PUT')<div class="row g-3"><div class="col-md-6"><label class="form-label">Amount</label><input type="number" step="0.01" min="0.01" name="amount" class="form-control" value="{{ old('amount', $refund->amount) }}" required></div><div class="col-md-6"><label class="form-label">Reason</label><input name="reason" class="form-control" value="{{ old('reason', $refund->reason) }}" required></div><div class="col-12"><label class="form-label">Note</label><textarea name="note" rows="5" class="form-control">{{ old('note', $refund->note) }}</textarea></div></div><div class="d-flex gap-2 mt-4"><button class="btn btn-primary"><i class="bi bi-check-circle me-2"></i>Save changes</button><a href="{{ route('admin.refunds.show', $refund) }}" class="btn btn-light border">Cancel</a></div></form></div></div>
@endsection
