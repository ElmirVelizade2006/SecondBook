@extends('layout.admin.master')
@section('title', 'Create Refund')
@section('content')
<div class="dashboard-section refunds-page"><div class="dashboard-panel mb-4"><div class="panel-header"><div><span class="eyebrow">Payments recovery</span><h5>Create refund</h5><p class="text-muted mb-0">Register a customer refund request.</p></div><a href="{{ route('admin.refunds.index') }}" class="btn btn-light border"><i class="bi bi-arrow-left me-2"></i>Back</a></div></div><div class="dashboard-panel refund-form-panel"><form method="POST" action="{{ route('admin.refunds.store') }}">@csrf<div class="row g-4"><div class="col-12 col-lg-8"><label class="form-label">Order <span class="text-danger">*</span></label><select name="order_id" class="form-select" required><option value="">Select order</option>@foreach($orders as $order)<option value="{{ $order->id }}" @selected(old('order_id') == $order->id)>#{{ $order->order_number }} · {{ $order->user?->name }} · ${{ number_format($order->total_price, 2) }}</option>@endforeach</select>@error('order_id')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror<div class="row g-3 mt-1"><div class="col-md-6"><label class="form-label">Amount <span class="text-danger">*</span></label><input type="number" step="0.01" min="0.01" name="amount" class="form-control" value="{{ old('amount') }}" required>@error('amount')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label">Reason <span class="text-danger">*</span></label><input name="reason" class="form-control" value="{{ old('reason') }}" required>@error('reason')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror</div><div class="col-12"><label class="form-label">Note</label><textarea name="note" rows="5" class="form-control">{{ old('note') }}</textarea></div></div></div><div class="col-12 col-lg-4"><div class="refund-info-box"><span class="eyebrow">Workflow</span><h6>Pending request</h6><p>New refunds start as pending. An authorised admin can approve, reject or process them.</p><span class="refund-status status-pending"><i class="bi bi-circle-fill"></i>Pending</span></div></div></div><div class="d-flex gap-2 mt-4"><button class="btn btn-primary"><i class="bi bi-check-circle me-2"></i>Create refund</button><a href="{{ route('admin.refunds.index') }}" class="btn btn-light border">Cancel</a></div></form></div></div>
@endsection
